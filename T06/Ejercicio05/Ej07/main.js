function muestra() {
    let extra = document.getElementsByClassName("adicional oculto")[0];
    extra.className = "visible";
    let link = document.getElementsByClassName("enlace")[0];
    link.className = "oculto";
}